<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arielcast">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/arielcast"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArielCast">
        <activity
            android:name=".ShowLecture"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <activity android:name=".ShowCourse" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_lecturer"
            android:theme="@style/Theme.ArielCast.NoActionBar" />
        <activity android:name=".AddLectureActivity" />
        <activity android:name=".StudentActivity"
            android:label="@string/title_activity_lecturer"
            android:theme="@style/Theme.ArielCast.NoActionBar" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".AddCourseActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>